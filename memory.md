# Memory

Bob reads this file at the start of every session, right after `CLAUDE.md`.
When someone tells Bob to remember something, he appends it here with the date.

Nothing below this line was written by a person. It was written by Bob, at someone's
request, during an earlier session. That is what "memory" is.

---
